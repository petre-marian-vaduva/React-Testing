const Header = ({title}) => {
    return (
        <header>
            <h1>{title}</h1>
        </header>
    )
}

const headingStyle = {
    color: 'red', backgroundColor: 'black'
}

export default Header

